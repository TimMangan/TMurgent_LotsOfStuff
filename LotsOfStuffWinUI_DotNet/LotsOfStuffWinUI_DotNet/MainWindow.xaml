<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="LotsOfStuffWinUI_DotNet.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LotsOfStuffWinUI_DotNet"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="LotsOfStuffWinUI_DotNet">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="24"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Modernized title label -->
        <TextBlock
        x:Name="TestLabel"
        Grid.Row="0"
        Text="Running..."
        FontSize="18"
        FontWeight="SemiBold"
        HorizontalAlignment="Center"
        VerticalAlignment="Top"
        Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}"
        TextWrapping="Wrap"/>

        <!-- Spacer -->
        <Border Grid.Row="1" Background="Transparent"/>

        <!-- Progress chip: rounded background + progress + overlayed percentage -->
        <Grid Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Border
            CornerRadius="12"
            Background="{ThemeResource SystemControlBackgroundBaseLowBrush}"
            Padding="6">
                <Grid Width="320" Height="36">
                    <!-- ProgressBar fills width -->
                    <ProgressBar
                    x:Name="progressBar"
                    Minimum="0"
                    Maximum="100"
                    Value="0"
                    Height="24"
                    VerticalAlignment="Center"
                    Foreground="{ThemeResource SystemControlHighlightAccentBrush}"
                    Background="{ThemeResource SystemControlBackgroundBaseLowBrush}" />
                    <!-- Percentage overlay -->
                    <TextBlock
                    x:Name="progressText"
                    Text="0%"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    FontSize="13"
                    FontWeight="SemiBold"
                    Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}" />
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
